import { AcademicYearPipe } from './academic-year.pipe';

describe('AcademicYearPipe', () => {
  it('create an instance', () => {
    const pipe = new AcademicYearPipe();
    expect(pipe).toBeTruthy();
  });
});
